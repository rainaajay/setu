app = "setu-economy"
primary_region = "lhr"

[build]
  dockerfile = "../../Dockerfile.economy"

[env]
  SETU_STATE_DIR = "/data"

[mounts]
  source = "setu_economy_data"
  destination = "/data"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
